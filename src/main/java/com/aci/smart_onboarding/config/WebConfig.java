package com.aci.smart_onboarding.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
  // No need for CORS configuration here as it's handled in BaseSecurityConfig
}
